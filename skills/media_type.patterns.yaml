# skill_hash: d9450e771aadfa0b
# Auto-generated by dynamoui scaffold. Review triggers and extend before committing.
entity: MediaType
patterns:
- id: media_type.list_all
  description: List all MediaType records
  triggers:
  - all media_types
  - show all media_types
  - list media_types
  - get all media_types
  query_template: '{"filters": []}'
  params: []
- id: media_type.by_name
  description: Search MediaType by Name
  triggers:
  - mediatype name
  - find mediatype by name
  - search mediatype name
  query_template: '{"filters": [{"field": "name", "op": "like", "value": "{name}"}]}'
  params:
  - name: name
    type: string
    required: true
- id: mediatype.tracks_by_format
  description: Find all tracks in a specific media format with their details.
  triggers:
  - show me all MP3 tracks
  - what tracks are available in MPEG format
  - list tracks by media type
  - find all songs in WAV format
  - which tracks use this media type
  query_template: '{"filters": [{"field": "media_type.name", "operator": "equals",
    "value": "{media_type_name}"}], "joins": [{"entity": "Track", "type": "inner",
    "on": "media_type.media_type_id = track.media_type_id"}], "sort": [{"field": "track.name",
    "direction": "asc"}], "result_limit": 100}'
  params:
  - name: media_type_name
    type: string
    required: true
    default: null
- id: mediatype.format_popularity
  description: Rank media formats by number of tracks and total revenue generated.
  triggers:
  - which media format has the most tracks
  - rank media types by popularity
  - show media formats sorted by track count
  - what format generates the most revenue
  - media type usage statistics
  query_template: '{"joins": [{"entity": "Track", "type": "left", "on": "media_type.media_type_id
    = track.media_type_id"}, {"entity": "InvoiceLine", "type": "left", "on": "track.track_id
    = invoice_line.track_id"}], "aggregations": [{"function": "count", "field": "track.track_id",
    "alias": "track_count"}, {"function": "sum", "field": "invoice_line.quantity",
    "alias": "total_sold"}, {"function": "sum", "field": "invoice_line.unit_price
    * invoice_line.quantity", "alias": "total_revenue"}], "group_by": ["media_type.media_type_id",
    "media_type.name"], "sort": [{"field": "track_count", "direction": "desc"}], "result_limit":
    50}'
  params: []
