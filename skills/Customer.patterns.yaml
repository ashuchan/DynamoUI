# skill_hash: e69534386ba03b60
# Auto-generated by dynamoui scaffold. Review triggers and extend before committing.
entity: Customer
patterns:
- id: customer.list_all
  description: List all Customer records
  triggers:
  - all customers
  - show all customers
  - list customers
  - get all customers
  query_template: '{"filters": []}'
  params: []
- id: customer.by_first_name
  description: Search Customer by First Name
  triggers:
  - customer first name
  - find customer by first name
  - search customer first name
  query_template: '{"filters": [{"field": "first_name", "op": "like", "value": "{first_name}"}]}'
  params:
  - name: first_name
    type: string
    required: true
- id: customer.by_last_name
  description: Search Customer by Last Name
  triggers:
  - customer last name
  - find customer by last name
  - search customer last name
  query_template: '{"filters": [{"field": "last_name", "op": "like", "value": "{last_name}"}]}'
  params:
  - name: last_name
    type: string
    required: true
- id: customer.by_postal_code
  description: Search Customer by Postal Code
  triggers:
  - customer postal code
  - find customer by postal code
  - search customer postal code
  query_template: '{"filters": [{"field": "postal_code", "op": "like", "value": "{postal_code}"}]}'
  params:
  - name: postal_code
    type: string
    required: true
- id: customer.by_email
  description: Search Customer by Email
  triggers:
  - customer email
  - find customer by email
  - search customer email
  query_template: '{"filters": [{"field": "email", "op": "like", "value": "{email}"}]}'
  params:
  - name: email
    type: string
    required: true
- id: customer.with_support_rep
  description: Show customers along with their assigned support representative details.
  triggers:
  - customers and their support reps
  - customer support staff assignments
  - who is the support representative for customers
  - customer with assigned employee
  query_template: '{"filters": {}, "sort": [{"field": "customer.customer_id", "direction":
    "asc"}], "joins": [{"entity": "Employee", "on": "customer.support_rep_id = employee.employee_id",
    "type": "left"}], "aggregations": [], "group_by": [], "result_limit": 100}'
  params: []
- id: customer.by_country
  description: Find customers located in a specific country.
  triggers:
  - customers in country
  - find customers by country
  - list customers from specific location
  query_template: '{"filters": [{"field": "customer.country", "operator": "=", "value":
    "{country}"}], "sort": [{"field": "customer.last_name", "direction": "asc"}],
    "joins": [], "aggregations": [], "group_by": [], "result_limit": 100}'
  params:
  - name: country
    type: string
    required: true
    default: null
- id: customer.by_company
  description: Filter customers by their company name.
  triggers:
  - customers from company
  - find customers by company
  - company employees as customers
  query_template: '{"filters": [{"field": "customer.company", "operator": "ilike",
    "value": "%{company_name}%"}], "sort": [{"field": "customer.last_name", "direction":
    "asc"}], "joins": [], "aggregations": [], "group_by": [], "result_limit": 50}'
  params:
  - name: company_name
    type: string
    required: true
    default: null
