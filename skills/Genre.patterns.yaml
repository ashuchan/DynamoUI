# skill_hash: 1908b51b66ce995a
# Auto-generated by dynamoui scaffold. Review triggers and extend before committing.
entity: Genre
patterns:
- id: genre.list_all
  description: List all Genre records
  triggers:
  - all genres
  - show all genres
  - list genres
  - get all genres
  query_template: '{"filters": []}'
  params: []
- id: genre.by_name
  description: Search Genre by Name
  triggers:
  - genre name
  - find genre by name
  - search genre name
  query_template: '{"filters": [{"field": "name", "op": "like", "value": "{name}"}]}'
  params:
  - name: name
    type: string
    required: true
- id: genre.tracks_by_genre
  description: Find all tracks in a specific genre with track details.
  triggers:
  - show me all tracks in the rock genre
  - what tracks belong to jazz
  - list songs by genre
  - find all rock music
  - tracks in metal genre
  query_template: '{"filters": [{"field": "genre.name", "operator": "equals", "value":
    "{genre_name}"}], "joins": [{"entity": "Track", "on": "genre.genre_id = track.genre_id",
    "type": "inner"}], "sort": [{"field": "track.name", "direction": "asc"}], "result_limit":
    100}'
  params:
  - name: genre_name
    type: string
    required: true
    default: null
- id: genre.genre_track_count
  description: Count how many tracks exist in each genre, ranked by popularity.
  triggers:
  - which genres have the most tracks
  - top genres by number of songs
  - genre popularity ranking
  - how many tracks per genre
  - genres ranked by track count
  query_template: '{"joins": [{"entity": "Track", "on": "genre.genre_id = track.genre_id",
    "type": "left"}], "group_by": ["genre.genre_id", "genre.name"], "aggregations":
    [{"function": "count", "field": "track.track_id", "alias": "track_count"}], "sort":
    [{"field": "track_count", "direction": "desc"}], "result_limit": 50}'
  params: []
