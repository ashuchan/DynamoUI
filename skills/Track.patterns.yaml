# skill_hash: d43dcd0ef4206cbd
# Auto-generated by dynamoui scaffold. Review triggers and extend before committing.
entity: Track
patterns:
- id: track.list_all
  description: List all Track records
  triggers:
  - all tracks
  - show all tracks
  - list tracks
  - get all tracks
  query_template: '{"filters": []}'
  params: []
- id: track.by_name
  description: Search Track by Name
  triggers:
  - track name
  - find track by name
  - search track name
  query_template: '{"filters": [{"field": "Name", "op": "ilike", "value": "{name}"}]}'
  params:
  - name: name
    type: string
    required: true
