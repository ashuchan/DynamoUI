# skill_hash: 3aafc365800da99a
# Auto-generated by dynamoui scaffold. Review triggers and extend before committing.
entity: Track
patterns:
- id: track.list_all
  description: List all Track records
  triggers:
  - all tracks
  - show all tracks
  - list tracks
  - get all tracks
  query_template: '{"filters": []}'
  params: []
- id: track.by_name
  description: Search Track by Name
  triggers:
  - track name
  - find track by name
  - search track name
  query_template: '{"filters": [{"field": "name", "op": "like", "value": "{name}"}]}'
  params:
  - name: name
    type: string
    required: true
- id: track.tracks_by_genre_with_album
  description: Find tracks in a genre with album and artist information.
  triggers:
  - show me all rock tracks with albums
  - jazz songs and their albums
  - tracks in pop genre with artist
  - find metal tracks and album names
  - list songs by genre with album details
  query_template: '{"filters": [{"field": "genre.name", "operator": "equals", "value":
    "{genre_name}"}], "joins": [{"entity": "Genre", "on": "track.genre_id = genre.genre_id",
    "type": "inner"}, {"entity": "Album", "on": "track.album_id = album.album_id",
    "type": "left"}, {"entity": "Artist", "on": "album.artist_id = artist.artist_id",
    "type": "left"}], "sort": [{"field": "track.name", "direction": "asc"}], "result_limit":
    500}'
  params:
  - name: genre_name
    type: string
    required: true
    default: null
- id: track.most_expensive_tracks
  description: Find the most expensive tracks by unit price.
  triggers:
  - what are the most expensive songs
  - highest priced tracks
  - most costly music
  - top 10 expensive tracks
  - show me premium priced songs
  query_template: '{"sort": [{"field": "track.unit_price", "direction": "desc"}],
    "result_limit": 20}'
  params: []
- id: track.long_songs
  description: Find tracks longer than a specific duration in milliseconds.
  triggers:
  - show me songs longer than 5 minutes
  - find long tracks over 300 seconds
  - what songs are more than 10 minutes
  - epic long tracks
  - songs exceeding 6 minute duration
  query_template: '{"filters": [{"field": "track.milliseconds", "operator": "gte",
    "value": "{min_milliseconds}"}], "sort": [{"field": "track.milliseconds", "direction":
    "desc"}], "result_limit": 100}'
  params:
  - name: min_milliseconds
    type: integer
    required: true
    default: '300000'
