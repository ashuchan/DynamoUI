# skill_hash: b1ee340ee6c0b9bf
# Auto-generated by dynamoui scaffold. Review triggers and extend before committing.
entity: Playlist
patterns:
- id: playlist.list_all
  description: List all Playlist records
  triggers:
  - all playlists
  - show all playlists
  - list playlists
  - get all playlists
  query_template: '{"filters": []}'
  params: []
- id: playlist.by_name
  description: Search Playlist by Name
  triggers:
  - playlist name
  - find playlist by name
  - search playlist name
  query_template: '{"filters": [{"field": "name", "op": "like", "value": "{name}"}]}'
  params:
  - name: name
    type: string
    required: true
- id: playlist.playlist_with_tracks
  description: Get all tracks in a specific playlist with track details.
  triggers:
  - show me the rock playlist songs
  - what tracks are in my favorites playlist
  - list all songs in workout playlist
  - get playlist contents for meditation
  - show tracks in the road trip playlist
  query_template: '{"filters": [{"field": "playlist.playlist_id", "operator": "equals",
    "value": "{playlist_id}"}], "joins": [{"entity": "PlaylistTrack", "on": "playlist.playlist_id
    = playlist_track.playlist_id", "type": "inner"}, {"entity": "Track", "on": "playlist_track.track_id
    = track.track_id", "type": "inner"}], "sort": [{"field": "track.name", "direction":
    "asc"}], "result_limit": 1000}'
  params:
  - name: playlist_id
    type: integer
    required: true
    default: null
- id: playlist.playlist_track_count
  description: Count songs in each playlist, ranked by size.
  triggers:
  - which playlists have the most songs
  - biggest playlists by track count
  - playlist popularity ranking
  - how many tracks in each playlist
  - playlists ranked by size
  query_template: '{"joins": [{"entity": "PlaylistTrack", "on": "playlist.playlist_id
    = playlist_track.playlist_id", "type": "left"}], "group_by": ["playlist.playlist_id",
    "playlist.name"], "aggregations": [{"function": "count", "field": "playlist_track.track_id",
    "alias": "track_count"}], "sort": [{"field": "track_count", "direction": "desc"}],
    "result_limit": 50}'
  params: []
