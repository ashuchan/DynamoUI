# skill_hash: f4524f2cc2d1ee92
# Auto-generated by dynamoui scaffold. Review triggers and extend before committing.
entity: Invoice
patterns:
- id: invoice.list_all
  description: List all Invoice records
  triggers:
  - all invoices
  - show all invoices
  - list invoices
  - get all invoices
  query_template: '{"filters": []}'
  params: []
- id: invoice.by_billing_postal_code
  description: Search Invoice by Billing Postal Code
  triggers:
  - invoice billing postal code
  - find invoice by billing postal code
  - search invoice billing postal code
  query_template: '{"filters": [{"field": "billing_postal_code", "op": "like", "value":
    "{billing_postal_code}"}]}'
  params:
  - name: billing_postal_code
    type: string
    required: true
- id: invoice.with_customer_info
  description: Retrieve invoices along with customer details and names.
  triggers:
  - invoices with customer details
  - show customer information with invoices
  - invoice and customer lookup
  query_template: '{"filters": {}, "sort": [{"field": "invoice.invoice_date", "direction":
    "desc"}], "joins": [{"entity": "Customer", "on": "invoice.customer_id = customer.customer_id",
    "type": "inner"}], "aggregations": [], "group_by": [], "result_limit": 100}'
  params: []
- id: invoice.total_by_customer
  description: Calculate total invoice amount for each customer, ranked by spending.
  triggers:
  - total sales per customer
  - customer spending ranking
  - top customers by revenue
  - customer lifetime value
  query_template: '{"filters": [], "sort": [{"field": "total_spent", "direction":
    "desc"}], "joins": [{"entity": "Customer", "on": "invoice.customer_id = customer.customer_id",
    "type": "inner"}], "aggregations": [{"function": "sum", "field": "invoice.total",
    "alias": "total_spent"}], "group_by": ["customer.customer_id", "customer.first_name",
    "customer.last_name"], "result_limit": 50}'
  params: []
- id: invoice.by_date_range
  description: Find invoices within a specific date range.
  triggers:
  - invoices between dates
  - invoices from date to date
  - recent invoices
  query_template: '{"filters": [{"field": "invoice.invoice_date", "operator": ">=",
    "value": "{start_date}"}, {"field": "invoice.invoice_date", "operator": "<=",
    "value": "{end_date}"}], "sort": [{"field": "invoice.invoice_date", "direction":
    "desc"}], "joins": [], "aggregations": [], "group_by": [], "result_limit": 100}'
  params:
  - name: start_date
    type: date
    required: true
    default: null
  - name: end_date
    type: date
    required: true
    default: null
