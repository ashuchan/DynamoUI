# skill_hash: 62445f9db7379866
# Auto-generated by dynamoui scaffold. Review triggers and extend before committing.
entity: Employee
patterns:
- id: employee.list_all
  description: List all Employee records
  triggers:
  - all employees
  - show all employees
  - list employees
  - get all employees
  query_template: '{"filters": []}'
  params: []
- id: employee.by_last_name
  description: Search Employee by Last Name
  triggers:
  - employee last name
  - find employee by last name
  - search employee last name
  query_template: '{"filters": [{"field": "last_name", "op": "like", "value": "{last_name}"}]}'
  params:
  - name: last_name
    type: string
    required: true
- id: employee.by_first_name
  description: Search Employee by First Name
  triggers:
  - employee first name
  - find employee by first name
  - search employee first name
  query_template: '{"filters": [{"field": "first_name", "op": "like", "value": "{first_name}"}]}'
  params:
  - name: first_name
    type: string
    required: true
- id: employee.by_title
  description: Search Employee by Title
  triggers:
  - employee title
  - find employee by title
  - search employee title
  query_template: '{"filters": [{"field": "title", "op": "like", "value": "{title}"}]}'
  params:
  - name: title
    type: string
    required: true
- id: employee.by_postal_code
  description: Search Employee by Postal Code
  triggers:
  - employee postal code
  - find employee by postal code
  - search employee postal code
  query_template: '{"filters": [{"field": "postal_code", "op": "like", "value": "{postal_code}"}]}'
  params:
  - name: postal_code
    type: string
    required: true
- id: employee.by_email
  description: Search Employee by Email
  triggers:
  - employee email
  - find employee by email
  - search employee email
  query_template: '{"filters": [{"field": "email", "op": "like", "value": "{email}"}]}'
  params:
  - name: email
    type: string
    required: true
- id: employee.with_manager
  description: Show employees with their direct manager or reporting relationship.
  triggers:
  - employees with their manager
  - who reports to whom
  - employee reporting structure
  - show manager for each employee
  query_template: '{"filters": [], "sort": [{"field": "employee.last_name", "direction":
    "asc"}], "joins": [{"entity": "Employee", "on": "employee.reports_to = manager.employee_id",
    "type": "left", "alias": "manager"}], "aggregations": [], "group_by": [], "result_limit":
    100}'
  params: []
- id: employee.hired_after_date
  description: Find employees hired after a specific date.
  triggers:
  - employees hired after
  - recent hires since date
  - staff hired in the last year
  query_template: '{"filters": [{"field": "employee.hire_date", "operator": ">=",
    "value": "{hire_date}"}], "sort": [{"field": "employee.hire_date", "direction":
    "desc"}], "joins": [], "aggregations": [], "group_by": [], "result_limit": 50}'
  params:
  - name: hire_date
    type: date
    required: true
    default: null
