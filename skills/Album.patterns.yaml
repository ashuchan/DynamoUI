# skill_hash: 3fd797008ba7d77b
# Auto-generated by dynamoui scaffold. Review triggers and extend before committing.
entity: Album
patterns:
- id: album.list_all
  description: List all Album records
  triggers:
  - all albums
  - show all albums
  - list albums
  - get all albums
  query_template: '{"filters": []}'
  params: []
- id: album.by_title
  description: Search Album by Title
  triggers:
  - album title
  - find album by title
  - search album title
  query_template: '{"filters": [{"field": "title", "op": "like", "value": "{title}"}]}'
  params:
  - name: title
    type: string
    required: true
- id: album.with_artist_details
  description: Find albums with their artist information joined together.
  triggers:
  - show me albums with artist names
  - albums and who created them
  - list all albums including artist details
  - albums by artist lookup
  query_template: '{"filters": {}, "sort": [{"field": "album.title", "direction":
    "asc"}], "joins": [{"entity": "Artist", "on": "album.artist_id = artist.artist_id",
    "type": "inner"}], "aggregations": [], "group_by": [], "result_limit": 100}'
  params: []
- id: album.count_by_artist
  description: Count how many albums each artist has in the catalog.
  triggers:
  - how many albums per artist
  - artist album count ranking
  - which artists have the most albums
  - album count by artist
  query_template: '{"filters": {}, "sort": [{"field": "album_count", "direction":
    "desc"}], "joins": [{"entity": "Artist", "on": "album.artist_id = artist.artist_id",
    "type": "inner"}], "aggregations": [{"function": "count", "field": "album.album_id",
    "alias": "album_count"}], "group_by": ["artist.artist_id", "artist.name"], "result_limit":
    50}'
  params: []
