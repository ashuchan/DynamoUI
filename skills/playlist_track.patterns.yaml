# skill_hash: 518a0b5bf5082d0b
# Auto-generated by dynamoui scaffold. Review triggers and extend before committing.
entity: PlaylistTrack
patterns:
- id: playlist_track.list_all
  description: List all PlaylistTrack records
  triggers:
  - all playlist_tracks
  - show all playlist_tracks
  - list playlist_tracks
  - get all playlist_tracks
  query_template: '{"filters": []}'
  params: []
- id: playlisttrack.find_track_playlists
  description: Find all playlists that contain a specific track.
  triggers:
  - which playlists have the track bohemian rhapsody
  - show me playlists containing this song
  - find all playlists with track 5
  - which playlists include this track
  - list playlists for this track
  query_template: '{"filters": [{"field": "playlist_track.track_id", "operator": "equals",
    "value": "{track_id}"}], "joins": [{"entity": "Playlist", "on": "playlist_track.playlist_id
    = playlist.playlist_id", "type": "inner"}], "sort": [{"field": "playlist.name",
    "direction": "asc"}], "result_limit": 100}'
  params:
  - name: track_id
    type: integer
    required: true
    default: null
